<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project_VDNRV.securedmessaging">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewMessage"></activity>
        <activity android:name=".SentUserList" />
        <activity android:name=".SentMessageList" />
        <activity android:name=".Decryption" />
        <activity android:name=".RecievedMeassageLayout" />
        <activity android:name=".RecievedMeassageList" />
        <activity android:name=".Send_message" />
        <activity android:name=".Register" />
        <activity android:name=".User_name" />
        <activity android:name=".User_List" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>